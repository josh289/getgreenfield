# Airtable API Key for form submissions (server-side only)
# Get your API key from: https://airtable.com/account
# In Railway, set this environment variable with your actual API key
#
# NOTE: This key is used server-side by API routes (/api/contact, /api/early-access).
# It is NOT exposed to the client. Do not use the PUBLIC_ prefix.
AIRTABLE_API_KEY=your_airtable_api_key_here
